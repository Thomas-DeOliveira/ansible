---
- name: Create a test directory on Debian
  hosts: all
  become: yes
  tasks:
    - name: Ensure the /test directory exists
      file:
        path: /test
        state: directory
        mode: '0755'
